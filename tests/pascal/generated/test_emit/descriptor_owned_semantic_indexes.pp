unit u;
interface
type tcolor = (red, green);
var value : record field : longint; end;
const flag = true;
      letter = 'x';
      compared = 1 < 2;
      converted = longint(3);
implementation
end.
