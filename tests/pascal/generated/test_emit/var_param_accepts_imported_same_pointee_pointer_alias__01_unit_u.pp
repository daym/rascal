unit u;
interface
uses second;
procedure take(var p : Q);
var
  x : P;
implementation
procedure take(var p : Q);
begin
end;
procedure run;
type
  X = record end;
begin
  take(x);
end;
begin
  run;
end.
