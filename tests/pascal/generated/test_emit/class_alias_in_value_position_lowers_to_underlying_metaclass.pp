unit u;
interface
type
  texportbase = class
  end;
  texportalias = texportbase;
procedure registerit(c : tclass);
implementation
procedure registerit(c : tclass);
begin
end;
begin
  registerit(texportalias);
end.
